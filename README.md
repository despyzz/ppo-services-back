# 📚 PPO Services Backend

Полноценное Node.js + Express + SQLite приложение с JWT-авторизацией и bcrypt-хешированием паролей для управления документами.

## 🏗️ Технический стек

- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **SQLite** - база данных (better-sqlite3)
- **JWT** - авторизация (jsonwebtoken)
- **bcrypt** - хеширование паролей
- **Multer** - загрузка файлов
- **CORS** - кросс-доменные запросы

## 📂 Структура проекта

```
/project
├── server.js                 # Основной сервер
├── package.json              # Зависимости
├── db/
│   └── database.sqlite       # База данных SQLite
├── models/
│   ├── documentModel.js      # Модель документов
│   └── userModel.js         # Модель пользователей
├── controllers/
│   ├── documentController.js # Контроллер документов
│   └── authController.js     # Контроллер авторизации
├── routes/
│   ├── documentRoutes.js     # Роуты документов
│   └── authRoutes.js        # Роуты авторизации
├── middleware/
│   └── authMiddleware.js     # JWT middleware
├── documents/               # Папка для загруженных файлов
└── public/
    └── admin.html           # Админ-панель
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск сервера

```bash
# Обычный запуск
npm start

# Запуск в режиме разработки (с nodemon)
npm run dev
```

### 3. Доступ к приложению

- **Админ-панель**: http://localhost:3000/public/index.html
- **API**: http://localhost:3000

## 🔐 Авторизация

### Дефолтный администратор

При первом запуске создается дефолтный администратор:
- **Username**: `admin`
- **Password**: `admin123`

### API авторизации

```bash
# Авторизация
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'

# Регистрация нового пользователя
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username": "newuser", "password": "password123"}'

# Получение информации о пользователе
curl -X GET http://localhost:3000/auth/me \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 📡 API для документов

### Создание документа (требует авторизации)

```bash
curl -X POST http://localhost:3000/documents \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "title=Company Policy 2025" \
  -F "target=EMPLOYEE" \
  -F "file=@/path/to/document.pdf"
```

### Получение всех документов

```bash
# Все документы
curl -X GET http://localhost:3000/documents

# С фильтрацией по типу
curl -X GET "http://localhost:3000/documents?target=EMPLOYEE"
```

### Получение документа по ID

```bash
curl -X GET http://localhost:3000/documents/1
```

### Обновление документа (требует авторизации)

```bash
curl -X PUT http://localhost:3000/documents/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated Title", "target": "STUDENT"}'
```

### Удаление документа (требует авторизации)

```bash
curl -X DELETE http://localhost:3000/documents/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Статистика документов

```bash
curl -X GET http://localhost:3000/documents/stats
```

## 📋 Модель данных

### Document

```json
{
  "id": 1,
  "title": "Company Policy 2025",
  "target": "EMPLOYEE",
  "file": {
    "name": "policy.pdf",
    "mime_type": "application/pdf",
    "url": "/documents/policy.pdf",
    "size": 154323
  },
  "created_at": "2024-01-01T00:00:00.000Z",
  "updated_at": "2024-01-01T00:00:00.000Z"
}
```

### User

```json
{
  "id": 1,
  "username": "admin",
  "password_hash": "$2b$10$...",
  "created_at": "2024-01-01T00:00:00.000Z"
}
```

## 🌐 Админ-панель

Админ-панель доступна по адресу: http://localhost:3000/public/index.html

### Функционал админ-панели:

#### 📊 Главная страница
- **Общая статистика** - количество документов и пользователей
- **Быстрые действия** - переходы к основным функциям
- **Информация о пользователе** - отображение текущего пользователя

#### 📄 Управление документами
- **Авторизация** - вход в систему
- **Фильтрация** - по типу документов (EMPLOYEE/STUDENT)
- **Добавление документов** - загрузка новых файлов
- **Просмотр документов** - таблица со всеми документами
- **Редактирование** - модальное окно для изменения документов
- **Удаление** - удаление документов и файлов
- **Корректная кодировка** - правильное отображение кириллических имен файлов

#### 🎨 Улучшения интерфейса
- **Модульная архитектура** - разделение на отдельные файлы
- **Навигация по разделам** - удобное переключение между функциями
- **Модальные окна** - вместо простых alert для редактирования
- **Адаптивный дизайн** - корректная работа на всех устройствах

## 📁 Поддерживаемые типы файлов

- **PDF** - application/pdf
- **Word** - application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document
- **Текст** - text/plain
- **Изображения** - image/jpeg, image/png, image/gif

**Максимальный размер файла**: 10MB

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env` для настройки:

```env
PORT=3000
JWT_SECRET=your-secret-key-change-in-production
```

### CORS настройки

В файле `server.js` можно настроить CORS для конкретных доменов:

```javascript
app.use(cors({
  origin: ['http://localhost:3000', 'https://yourdomain.com'],
  methods: ['GET', 'POST', 'PUT', 'DELETE'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));
```

## 🛡️ Безопасность

- **JWT токены** с истечением через 1 час
- **bcrypt** для хеширования паролей
- **Валидация** входных данных
- **Фильтрация** типов файлов
- **Ограничение** размера файлов

## 📝 Примеры использования

### JavaScript (fetch)

```javascript
// Авторизация
const loginResponse = await fetch('/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'admin', password: 'admin123' })
});
const { token } = await loginResponse.json();

// Создание документа
const formData = new FormData();
formData.append('title', 'New Document');
formData.append('target', 'EMPLOYEE');
formData.append('file', fileInput.files[0]);

const createResponse = await fetch('/documents', {
  method: 'POST',
  headers: { 'Authorization': `Bearer ${token}` },
  body: formData
});
```

### Python (requests)

```python
import requests

# Авторизация
login_data = {'username': 'admin', 'password': 'admin123'}
response = requests.post('http://localhost:3000/auth/login', json=login_data)
token = response.json()['token']

# Получение документов
headers = {'Authorization': f'Bearer {token}'}
documents = requests.get('http://localhost:3000/documents', headers=headers)
```

## 🐛 Отладка

### Логи сервера

Сервер выводит подробные логи всех запросов:

```
[2024-01-01T12:00:00.000Z] POST /auth/login - IP: ::1
[2024-01-01T12:00:01.000Z] GET /documents - IP: ::1
```

### Проверка базы данных

База данных SQLite создается автоматически в папке `db/`. Можно использовать SQLite браузеры для просмотра данных.

## 📄 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к папкам
4. Убедитесь, что порт 3000 свободен

---

**Создано с ❤️ для управления документами PPO Services**
